---
description: Bootstrap a new project — CLAUDE.md, git init, private GitHub repo
argument-hint: [simple|detail|full]
allowed-tools: Skill(kickoff), Bash, Read, Write, Edit, AskUserQuestion
---

Invoke the `kickoff` skill to bootstrap the current directory as a new project.

The user passed mode argument: `$ARGUMENTS`

Pass this argument through to the skill's intake/routing. If the argument is empty or unrecognized, the skill will ask the user which mode (simple / detail / full) to use.
